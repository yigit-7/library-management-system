# library-management-system
Library Management System is a full-stack application built with Next.js (Frontend) and Spring Boot (Backend). It automates core library tasks, featuring secure JWT authentication, efficient book/user management, and an SQL database.
